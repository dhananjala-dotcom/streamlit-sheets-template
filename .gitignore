# Streamlit secrets — never commit
.streamlit/secrets.toml

# Service-account JSON keys, if any are ever downloaded locally
*service-account*.json
*service_account*.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# macOS
.DS_Store
